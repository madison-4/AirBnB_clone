#!/usr/bin/python3
""" A module to implement the entry point of the command interporeter
"""


import cmd
import re


class HBNBCommand(cmd.Cmd):
    """ A class to define the console part of the airbnb clone
    """

    prompt = "(hbnb)"
    storgae = models.storage
    
